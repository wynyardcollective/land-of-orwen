# Copy to keystore.properties and fill after generating the release keystore.
# NEVER commit keystore.properties or rough-release.keystore.

storeFile=rough-release.keystore
storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=rough
